# [프로젝트명]

[한두 줄 프로젝트 설명]

> **이 파일은 새 프로젝트용 README 스켈레톤입니다.**
> 부트스트랩 시 이 파일을 `README.md`로 복사해 실제 값으로 채우고, 이 `_skeleton-README.md`는
> 삭제하세요. (템플릿 레포 자체의 소개는 루트 `README.md`를 참조)
> 기능 변경 시 자동 동기화됩니다(규칙: `.claude/rules/readme-sync.md`).

## Features

- [기능 1]
- [기능 2]

## Prerequisites

> 외부 의존성은 **호환 검증된 버전**을 근거와 함께 고정합니다(근거는 DEC 로그).

- [런타임/언어 버전 — 예: Node.js 20 LTS, TypeScript 5.x]
- [필수 도구]

## Installation

```bash
[설치 명령]
```

## Usage

```bash
[기본 사용법 또는 주요 명령어]
```

## Configuration

| 환경 변수 | 설명 | 기본값 |
|-----------|------|--------|
| `VAR_NAME` | 설명 | `값` |

## Dependencies

| 패키지 | 버전 | 고정 근거(DEC) |
|--------|------|----------------|
| `예: tar` | `^6.2.1` | DEC-NNN — v7 ESM-only 비호환 |

## Changelog

> 외부 공개용 요약. 내부 상세 이력은 `.claude/workspace/changelog.md`.
> 이 섹션은 `/release <major|minor|patch>` 가 골격을 삽입합니다 — 손으로 버전을 맞추지 마세요.

### YYYY-MM-DD
- **feat**: 초기 릴리즈
