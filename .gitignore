# 개인 로컬 설정 (팀 공유 금지)
.claude/settings.local.json

# 의존성 / 빌드 산출물
node_modules/
dist/
build/
*.tsbuildinfo

# 환경 변수
.env
.env.local

# OS / 에디터
.DS_Store
*.log

# workspace/ 는 git에 포함한다(진행 현황 공유 목적) — 무시하지 않음
