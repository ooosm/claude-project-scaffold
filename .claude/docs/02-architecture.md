# 02 — 아키텍처 (Architecture)

> 현재 코드 구조의 스냅샷. **greenfield**는 첫 구조가 잡힌 시점(대략 Phase 1 완료)에 작성하고,
> **brownfield**는 도입 시 기존 코드를 역설계해 작성한다. 구조가 바뀌는 작업이 머지될 때마다 갱신한다.
> 큰 구조 결정의 "왜"는 ADR/DEC(`.claude/decisions/`)에 있고, 이 문서는 "현재 무엇이 어떻게"를 담는다.

_최종 업데이트: YYYY-MM-DD_

## 시스템 개요

[한 문단: 주요 구성 요소와 이들이 주고받는 것. 다이어그램(mermaid/ASCII) 권장]

## 모듈 구조

| 모듈/디렉토리 | 책임 | 주요 진입점 |
|---------------|------|-------------|
| `src/...` | [무엇을 담당] | `...` |

## 데이터 흐름

[요청/데이터가 시스템을 통과하는 경로. 저장소·큐·외부 API 경유 지점 명시]

## 외부 의존성

> 버전 고정 근거는 DEC 로그(→ `.claude/rules/decisions.md`), 표기는 README Dependencies.

| 의존성 | 버전 | 역할 | 근거 |
|--------|------|------|------|
| [예: tar] | `^6.2.1` | ... | DEC-NNN |

## 배포/실행 구조

[로컬 실행, 빌드 산출물, 배포 대상 환경. 실행 명령은 `.claude/rules/commands.md` 참조]
